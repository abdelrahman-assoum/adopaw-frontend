import React from 'react';
import TabNavigator from './TabNavigator';  // <-- import the bottom tab navigator

const RootNavigator = () => {
  return <TabNavigator />;   // <-- replace the stack with tabs
};

export default RootNavigator;
// This file serves as the entry point for the app's navigation structure,